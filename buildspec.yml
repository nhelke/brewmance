version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  # install:
    # commands:
      #  - bundle install
      # - command
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
       - git archive --format=tar "--prefix=_site/" "$CODEBUILD_RESOLVED_SOURCE_VERSION" | tar -x
      # - command
  post_build:
    commands:
       - aws s3 sync _site s3://www.brewmance.uk --delete
# artifacts:
  # files:
    # - '**/*'
    # - location
  #discard-paths: yes
  # base-directory: out